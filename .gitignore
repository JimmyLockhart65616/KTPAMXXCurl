# Local dev files (not committed)
nul

# Claude Code per-machine settings. NOT a blanket .claude/ ignore —
# .claude/skills/ is deliberately tracked.
.claude/settings.local.json

# SSH/deployment scripts (contain IPs and credentials)
scripts/

# Build outputs
bin/
build/
obj/

# IDE and editor files
.vs/
.vscode/
*.vcxproj.user
*.suo
*.user
*.sln.docstates

# Atom build config
.atom-build.json

# Compiled binaries
*.dll
*.so
*.pdb
*.ilk
*.exp
*.lib
*.o
*.obj

# Dependencies (if vendored locally during build)
# Note: deps/ is committed as it contains required headers

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
